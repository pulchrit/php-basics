<?php

  // define it (function names are cas INsensitive myFunc = myfunc)
  function myFunc($name) {
    echo "hello, $name, from function <br>";
  }

  myFunc('Ezzie'); // call it
  myFUnc('Ada');
?>